{{- if .Values.persistence.home.enabled -}}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "synapse-ai.homePVC" . }}
  namespace: {{ include "synapse-ai.namespace" . }}
  labels:
    {{- include "synapse-ai.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.persistence.home.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.persistence.home.size | quote }}
  {{- with .Values.persistence.home.storageClass }}
  storageClassName: {{ . | quote }}
  {{- end }}
{{- end -}}
